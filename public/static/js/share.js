// JavaScript Document
$(document).ready(function(e) {
	var Heigts=$(window).height();
	$('.choice').click(function(){
		if($(this).hasClass('chico_bg')){
			$(this).removeClass('chico_bg');
		}else{
			$(this).addClass('chico_bg');
		};
	});
	$('.coll_list').find('.coll_ico').each(function(index, element) {
		$(this).click(function(){
			if($(this).hasClass('coll_bg')){
				$(this).removeClass('coll_bg');
			}else{
				$(this).addClass('coll_bg');
			}
		});
    });
	$('.coll_btn').find('.sc').click(function(){
		$(this).parents('.coll_btn').find('.sc_js').removeClass('hidden');
	})
	$('.coll_btn').find('.qk').click(function(){
		$(this).parents('.coll_btn').find('.qk_js').removeClass('hidden');
	})
	$('.tptj').find('.mil_img').each(function(index, element) {
        if((index+1)%2==0){
			$(this).css({'float':'right'});
		}
    });
	$('.ShareBox').find('.fx_ico').click(function(){
		$(this).parents('.ShareBox').find('.coll_tc').removeClass('hidden')
	});
	$('.ShareBox').find('.s_colse').click(function(){
		$(this).parents('.ShareBox').find('.coll_tc').addClass('hidden');
	});
	$('.reply').find('.fb').click(function(){
		$(this).parents('.reply').find('.succeed').addClass('hida');
		//setTimeout("$('.succeed').hide()",2000);
	});
	$('.reply').find('.fb').click(function(){
		$(this).parents('.reply').find('.fail').addClass('hida');
		//setTimeout("$('.fail').hide()",2000);
	});
	$('.navis').find('.drawer').click(function(){
		if($(this).hasClass('menu_bg')){
			$(this).removeClass('menu_bg');
		}else{
			$(this).addClass('menu_bg');
		}
		if($(this).siblings('.subNav').hasClass('hidden')){
			$(this).siblings('.subNav').removeClass('hidden');
			$(this).parents('.WholeBox').css({'height':Heigts,'overflow':'hidden'});
		}else{
			$(this).siblings('.subNav').addClass('hidden');
			$(this).parents('.WholeBox').css({'height':'auto'});
		};
	});
	/*$('.naviLi').find('.n_new').click(function(){
		if($(this).parents('.naviLi').siblings('.subNav').hasClass('hidden')){
			$(this).parents('.naviLi').siblings('.subNav').removeClass('hidden');
		}else{
			$(this).parents('.naviLi').siblings('.subNav').addClass('hidden');
		};
		if($(this).parents('.naviLi').siblings('.drawer').hasClass('menu_bg')){
			$(this).parents('.naviLi').siblings('.drawer').removeClass('menu_bg');
		}else{
			$(this).parents('.naviLi').siblings('.drawer').addClass('menu_bg');
		}
	});*/
	$('.header').find('.user').click(function(){
		if($(this).parents('.header').find('.fitBox').hasClass('hidden')){
			$(this).parents('.header').find('.fitBox').show();
		}else{
			$(this).parents('.header').find('.fitBox').hide();
		};
	});
	$('.fitBox').find('.shez').click(function(){
		if($(this).parents('.fitBox').find('.putCon').hasClass('hidden')){
			$(this).parents('.fitBox').find('.putCon').removeClass('hidden');
			$(this).parents('.WholeBox').css({'height':Heigts,'overflow':'hidden'});
		}else{
			$(this).parents('.fitBox').find('.putCon').addClass('hidden');
			$(this).parents('.WholeBox').css({'height':'auto'});
		};
	});
	$('.MenuList').find('.it_new').click(function(){
		if($(this).parents('.nav_list').siblings('.subList').hasClass('hidden')){
			$(this).parents('.nav_list').siblings('.subList').removeClass('hidden');
		}else{
			$(this).parents('.nav_list').siblings('.subList').addClass('hidden');	
		}
		if($(this).hasClass('hover')){
			$(this).removeClass('hover');
		}else{
			$(this).addClass('hover');	
		}
	});
	$('.list_js li').each(function(index, element) {
        if((index+1)%2==0){
			$(this).css({'float':'right'});
		}
    });
	$('.deviceBox').find('.icos_fx').click(function(){
		if($(this).siblings('.coll_tc').hasClass('hidden')){
			$(this).siblings('.coll_tc').removeClass('hidden');
		}else{
			$(this).siblings('.coll_tc').addClass('hidden');	
		}
	});
	$('.deviceBox').find('.s_colse').click(function(){
		$(this).parents('.deviceBox').find('.coll_tc').addClass('hidden');
	});
	$('.sub_nav').find('.menu').click(function(){
		if($(this).parents('.head_nav').siblings('.subNav').hasClass('hidden')){
			$(this).parents('.head_nav').siblings('.subNav').show();
		}else{
			$(this).parents('.head_nav').siblings('.subNav').hide();
		}
		
	});
	$('.sub_nav .menu,.header .user').click(function(){
		jQuery(window).scroll(function(){
			if(jQuery(".subNav").css("display")!="none"){//弹出窗口时不允许滚动条滚动
				jQuery(window).scrollTop(0);
			}
		});
		jQuery(window).scroll(function(){
			if(jQuery(".fitBox").css("display")!="none"){//弹出窗口时不允许滚动条滚动
				jQuery(window).scrollTop(0);
			}
		});
	})
	$('.coll_bg').click(function(){
		jQuery(window).scroll(function(){
			if(jQuery(".subNav").css("display")="none"){//弹出窗口时不允许滚动条滚动
				jQuery(window).scrollTop();
			}
		});
		jQuery(window).scroll(function(){
			if(jQuery(".fitBox").css("display")="none"){//弹出窗口时不允许滚动条滚动
				jQuery(window).scrollTop();
			}
		});
	});
	$('.date_edi').click(function(){
		if($(this).siblings('.box_det').hasClass('hidden')){
			$(this).siblings('.box_det').removeClass('hidden');
		}else{
			$(this).siblings('.box_det').addClass('hidden');
		}
	});
	$('.coll_bg_a').click(function(){
		$(this).parents('.subNav').addClass('hidden');
		$(this).parents('.subNav').siblings('.drawer').removeClass('menu_bg');	
		$(this).parents('.WholeBox').css({'height':'auto'});
	});
	$('.coll_bg').click(function(){
		$(this).parents('.fitBox').hide();
		$(this).parents('.subNav').hide();
		$(this).parents('.coll_tc').addClass('hidden');
		$(this).parents('.WholeBox').css({'height':'auto'});
	});
	
	$('.top_sj').click(function(){
		if($(this).parents('.top_tit').siblings('.top_img').hasClass('hidden')){
			$(this).parents('.top_tit').siblings('.top_img').removeClass('hidden');
			$(this).removeClass('top_sja');
		}else{
			$(this).parents('.top_tit').siblings('.top_img').addClass('hidden');
			$(this).addClass('top_sja');
		}
	});
	//0728
	$('.sc_ico').click(function(){
		if($(this).hasClass('hover_a')){
			$(this).removeClass('hover_a');
		}else{
			$(this).addClass('hover_a');
		}
	});
	$('.js_ico').click(function(){
		var hmiao=500;
		$(this).addClass('hover');
		$(this).find('.d_ico_a').removeClass('hid');
		$(this).find('.d_ico_a').animate({
			top:'-40px',
			opacity:'0'
		},hmiao)
	});
	$('.icos_sc').click(function(){
		if($(this).hasClass('hover')){
			$(this).removeClass('hover');
		}else{
			$(this).addClass('hover');
		}
	});
	//0814新增js
	$('.btn_tj').click(function(){
		if($(this).hasClass('btn_tj_a')){
			$(this).removeClass('btn_tj_a');
			$(this).parents('.atlas_top').siblings('.textConDtlas').show();
			$(this).parents('.atlas_top').siblings('.textConList').hide();
		}else{
			$(this).addClass('btn_tj_a');
			$(this).parents('.atlas_top').siblings('.textConDtlas').hide();
			$(this).parents('.atlas_top').siblings('.textConList').show();
		}
	});
	$('.textListBox li').each(function(index, element) {
        if((index+1)%2==0){
			$(this).css({'float':'right'});
		}
    });
});